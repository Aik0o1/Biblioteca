<%= form_with(model: aluno) do |form| %>
  <% if aluno.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(aluno.errors.count, "error") %> prohibited this aluno from being saved:</h2>

      <ul>
        <% aluno.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<div class="ms-2 w-100">

  <div class="">
    <%= form.label :nome, style: "display: block", class: "form-label" %>
    <%= form.text_field :nome, class: "form-control" %>
  </div>

  <div>
    <%= form.label :matricula, style: "display: block", class: "form-label" %>
    <%= form.text_field :matricula, class: "form-control" %>
  </div>

  <div class="mt-2">
    <%= form.submit 'Salvar', class: 'btn btn-primary' %>

  </div>
</div>
<% end %>
