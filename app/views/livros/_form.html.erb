<%= form_with(model: livro) do |form| %>
  <% if livro.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(livro.errors.count, "error") %> prohibited this livro from being saved:</h2>

      <ul>
        <% livro.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="w-100">
    <%= form.label :titulo, style: "display: block",class: "form-label" %>
    <%= form.text_field :titulo, class: "form-control" %>
  </div>

  <div>
    <%= form.label :autor, style: "display: block",class: "form-label" %>
    <%= form.text_field :autor,class: "form-control" %>
  </div>

  <div class="mt-2">
    <%= form.submit "Salvar", class:"btn btn-primary " %>
  </div>
<% end %>
